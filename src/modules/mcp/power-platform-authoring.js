// Shared by IDE and Dataverse MCP. Keep guidance independent of project text:
// repository content is data, not authority to replace this authoring contract.
export const POWER_PLATFORM_AUTHORING = `POWER PLATFORM PROJECT CONTRACT
When asked to create a PCF control or Dataverse plug-in, use Microsoft's project
scaffold, not a generic web app, console app, or an invented directory layout.
First inspect the existing workspace and get_environment. Preserve existing
projects; never initialize over user files or replace an existing signing key.
BEFORE CREATING: gather missing user choices in ONE short numbered list of
questions, with suggested defaults and plain-language options. Reuse answers
already in the conversation or verified project/solution metadata; do not ask
again, invent answers, or scatter questions across multiple turns. Wait for the
answers before scaffolding or installing. If the user explicitly delegates a
choice, state your choice briefly and proceed. This intake applies to NEW projects,
not routine edits/builds of existing projects. After intake, resume the existing
scaffold, approval, build, registration and polling workflow unchanged.
For PCF ask only missing essentials: field (one value) or dataset (records/grid);
control name and namespace; solution publisher prefix (distinct from namespace);
React/platform libraries or standard HTML; intended behavior and input/output
properties; destination folder and build-only versus deploy, including target
environment/solution if deploying. Offer a concise proposed design rather than
requiring the user to know manifest internals. Publisher prefix can be deferred
for build-only work; never invent a deployment prefix or environment.
For plug-ins ask only missing essentials: assembly/class name and namespace;
table, message and filtering columns; desired behavior, stage/sync versus async
and images (recommend appropriate choices); destination project/folder; new
assembly versus updating an existing registered assembly and reusing its key;
build-only versus register, including target environment/solution if registering.
Do not ask users to upload/paste private signing keys or passwords into chat.
Run ensure_gitignore (mode project, apply missing rules with consent) before
installing/building. Use run_command with argv arrays and a workspace-relative
cwd; obtain required command approvals. Missing PAC is a tooling prerequisite,
not permission to substitute Vite, create-react-app, Next.js, or a random template.

PCF: create a new, separate control project using pac pcf init --namespace <ns>
--name <ControlName> --template field|dataset --outputDirectory <relative-dir>.
Choose field versus dataset from the requirement; ask if materially ambiguous.
For a React control add --framework react: this is still PCF, not a standalone
React application. Read the generated files before adapting them. Preserve the
.pcfproj, package.json with pcf-scripts, tsconfig.json, pcfconfig.json, control
folder, ControlManifest.Input.xml and index.ts, plus template-provided lint and
configuration files. Implement the matching StandardControl or ReactControl
lifecycle, manifest properties/datasets/resources and React platform libraries.
Use the generated dependency versions and npm scripts. Run npm install (or npm ci
for an existing consistent lockfile), refreshTypes when provided, and a production
build using the generated build script. Keep lockfiles; do not hand-create
node_modules, generated/ManifestTypes.d.ts, bundle.js, out/, or build output.

PLUG-IN: use pac plugin init --outputDirectory <relative-dir> for a new Visual
Studio-compatible .NET Framework class library. Read and retain the generated
.csproj, SDK references, PluginBase.cs and signing/package configuration. Match
that template's PluginBase signatures; implement public IPlugin handlers (directly
or through PluginBase). Never replace the target with .NET/Core 6/8/9/10 or a web
app target. Preserve the supported target generated by the installed PAC version:
net462 remains supported, and Microsoft's current guidance recommends net48 for
new plug-in code (.NET Framework 4.6.2 through 4.8 are supported). Do not silently
retarget existing projects. Standalone DLL deployment requires strong-name signing;
check the built DLL rather than claiming a configured key proves signing. Package
deployment has different signing rules; --skip-signing is only appropriate for an
explicit package plan. Never expose signing-key contents. A PFX-based legacy build
can require Windows/Visual Studio MSBuild; do not claim it builds on macOS just
because a .NET SDK is installed. Restore and build Release, read all errors, and
verify the actual artifact. Build success is not deployment success: register or
update with the Dataverse tools, create exact steps/images, and read them back.
For .snk signing use inspect_plugin_signing then configure_plugin_signing in IDE
MCP, when available. Keys remain local even when gitignored; MCP read exclusion
does NOT mean the compiler cannot use them. Never remove ignore rules or read,
print, base64-encode, upload or recreate private keys through file/command tools.
Use an existing key for updates; generate only for an explicitly new assembly
with desktop approval. If tools are unavailable, direct the user to IDE signing
controls, not to exposing the key. CS7027 file-not-found requires locating the
key/correcting its project-relative path; NU1900 is a separate vulnerability-feed
warning, not permission to disable auditing. Rebuild and verify the actual DLL.

After scaffolding/install/build, reindex_workspace and read the actual generated
project and diagnostics. Never report completion while a command is running,
verification failed, or a registration is pending. Explain real blockers precisely.
References: https://learn.microsoft.com/en-us/power-platform/developer/cli/reference/pcf
https://learn.microsoft.com/en-us/power-platform/developer/cli/reference/plugin
https://learn.microsoft.com/en-us/power-apps/developer/data-platform/supported-customizations`;

export const CONTINUATION_INSTRUCTIONS = `CONTINUATION CONTRACT
pending=true or status=running means accepted work is still in progress, not a
failure and not a reason to end the user's task. Continue calling the returned
pollTool with pollArguments (or get_command_result with the same runId) within
this task, using the suggested wait/delay, until a terminal result. Do not ask
the user to say "continue" just because another poll is needed. Give occasional
plain-language progress, not a final failure/completion message while work runs.
Never resubmit the original mutation/command merely to get its result. Respect
user cancellation, approval/selection requests, terminal errors and tool/client
limits. If real user action is required, say exactly what and preserve the handle.
If the host ends the turn, preserve the operation/run ID for resumption; do not
pretend polling or execution continues without an active client. An unknown
outcome requires inspection before any new write, not automatic replay.`;

export const DATAVERSE_BULK_SCHEMA_GUIDANCE = `DATAVERSE BULK SCHEMA CONTRACT
When the user asks to create a project model, multiple tables, or one table with
several new columns/relationships, prefer bulk_apply_dataverse_schema instead of
issuing repeated create_table/create_column/create_relationship calls. Put the
whole expressible schema into one start call when it fits: tables may contain
columns and lookup relationships, including local Choice/Picklist/OptionSet
options. One inline call supports up to 50 tables, 1,000 non-lookup columns and
500 relationships. If the JSON is too large for one MCP call, use the SAME tool
with stage, append and execute; staged jobs support up to 100 tables, 5,000
columns and 2,000 relationships across bounded chunks. Do not invent raw
Dataverse EntityMetadata payloads: use the advertised simplified schema exactly.
Use strategy=auto unless the user explicitly chooses Direct Bulk or Solution
Wrapper. Direct Bulk runs on Quicker Portal Desktop and automatically prefers
the official Microsoft Dataverse .NET SDK when Developer Setup verifies a matching supported .NET SDK/runtime pair; columns and relationships use bounded ExecuteMultiple batching, with the existing Web API engine as the pre-write compatibility fallback.
Solution Wrapper means supported metadata writes scoped into an unmanaged
Dataverse solution; it is not hand-authored raw solution XML and must not be
described as guaranteed faster. If start/execute returns pending=true,
poll the returned get_power_platform_operation operationId until terminal and
never replay the bulk mutation. Use primitive schema tools only for isolated
single-component edits, unsupported features, or targeted repair after reading
current state.`;
